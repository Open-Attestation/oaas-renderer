import { TemplateRegistry } from '@govtechsg/decentralized-renderer-react-components'
import { csaMilestoneTemplates } from './milestone'

export const registry: TemplateRegistry<any> = {
    csaMilestone: csaMilestoneTemplates,
}
